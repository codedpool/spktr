{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main overlay window",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "shell:default",
    "screenshots:default"
  ]
}
